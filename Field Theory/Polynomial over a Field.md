In this section we investigate the properties of polynomial $f\in K[X]$ given some [[Field]] $K$. More about the general algebraic structure of polynomials can be found in [[Polynomial Ring]]. Note that given homomorphism $\phi:K\to K'$ for any $f\in K[X]$ we denote $\phi(f)=f^\phi$. 
- **Split**: $f\in K[X]$ is said to splits over $K$ if there exists $k,k_1\cdots,k_{\deg f}$ s.t. $$f(X)=k\prod_{i=1}^{\deg f}(X-k_i)$$This means that all zeros of $f$ in $K$ are exactly $k_i$.
- **Formal derivative**: for $f\in K[X]$ define the derivative as $$f(X)=\sum_{i=0}^na_iX^i\quad\Longrightarrow\quad f'(X)=\sum_{i=1}^nia_iX^{i-1}$$the usual properties from [[Differential Calculus on Euclidean Space]] can be verified directly.
## Splitting field
The **splitting field** of $f\in K[X]$ is an extension field $\Sigma/K$ ([[Field Extension]]) defined as $$\Sigma_{f/K}=\bigcap_{\Sigma'/K,f\text{ splits over }\Sigma'}\Sigma'$$i.e., it's the smallest extension of $K$ over which $f$ splits. The splitting field can actually be **uniquely (up to isomorphism)** constructed explicitly as $$\Sigma_{f/K}=K(\sigma_1,\cdots,\sigma_{\deg f})\where\sigma_i,1\le i\le\deg f\text{ are all roots of }f$$**Proof**: for existence, take $f\in K[X]$ and pick an irreducible $g\mid f$. Consider the quotient ([[Ideal]]) $$L=K[X]/(g(X)),\quad \pi:h\mapsto h\bmod g$$Since $(g(X))$ is a maximal [[Ideal]] by irreducibility ([[Integral Domain]]), $L$ is a field, and we can treat $L$ as an extension of $K$ because $\pi|_K$ is injective. Let $\alpha=\pi(X)$, then $$f(\alpha)=f(\pi(X))=\pi(f(X))=f\bmod g=0$$hence $\alpha$ is a root of $f$. By [[Field]] we have that $[L:K]\le\deg f=n$. 
	- **Degree of splitting field**: by induction we can easily show that there exists a splitting field $\Sigma$ of $f\in K[X]$ s.t. $[\Sigma:K]\le n!$.
- **Uniqueness up to isomorphism**: given isomorphism $\phi:K\to K'$ and $f\in K[X]$, denote the splitting fields of $f,f^\phi$ as $\Sigma,\Sigma'$, then there exists an isomorphism $\psi:\Sigma\to\Sigma'$ s.t. $\psi|_K=\phi$, i.e.,
  <p align="center"><img align="center" src="https://i.upmath.me/svg/%0A%5Cbegin%7Btikzcd%7D%0AK%20%5Carrow%5Br%2C%20%22%5Cphi%22%5D%20%5Carrow%5Bd%2C%20hook%5D%20%26%20K'%20%5Carrow%5Bd%2C%20hook%5D%20%5C%5C%0A%5CSigma%20%5Carrow%5Br%2C%20%22%5Cpsi%22'%5D%20%20%20%20%20%20%20%20%20%20%20%26%20%5CSigma'%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%0A%5Cend%7Btikzcd%7D%0A"/></p>
  **Proof**: we construct $\psi$ by induction on $\deg f$. Let $\sigma\in\Sigma$ be a root of $f$, then by invariance of division relation we have $\mu^\phi_{\sigma/K}$ irreducible over $K'$. Meanwhile, since $\mu^\phi_{\sigma/K}\mid f^\phi$ which splits over $L$, we pick $\sigma'\in\Sigma'$ a root of $\mu^\phi_{\sigma/K}$, then irreducibility implies $$\mu_{\sigma/K}^\phi=\mu_{\sigma'/K'}$$Now by isomorphic simple extension above there exists $\psi_1$ that with the desired properties. Now the induction assumption applied on $f/(X-\sigma)$ completes the proof. 
	- **Corollary**: taking $K'=K$ with $\phi=\id$, we have that any two splitting fields over $K$ are $K$-isomorphic. 
## Normal extension
[[Field]] extension $L/K$ is **normal** if every irreducible $f\in K[X]$ that has zero in $L$ splits over $L$. 
- **Example**: $\mathbb C/\mathbb R$ is normal by the fundamental theorem of algebra above, but $\mathbb Q(\sqrt[3]{2}):\mathbb Q$ is not, since $x^3-2$ does not splits over $\mathbb Q(\sqrt[3]{2})$.
- **Normality criterion**: assuming that $L/K$ is finite, then following are equivalent: 
  1. $L/K$ is normal.
  2. $L$ is the splitting field of some $f\in K[X]$. 
  3. Any $K$-homomorphism $\phi:L\to L'$, where $L'$ is any extension of $L$, is an automorphism of $L$.
  **Proof**: for $1\Rightarrow2$, by finiteness criterion ([[Field]]) we have $L=K(\ell_1,\cdots,\ell_s)$ with $\ell_i,\forall 1\le i\le s$ algebraic over $K$. Consider $$f=\prod_{i=1}^s\mu_{\ell_i/K}\quad\Longrightarrow\quad f(\ell_i)=0,\forall 1\le i\le s\quad\Longrightarrow\quad L\text{ is the splitting field of }f$$For $2\Rightarrow3$, for $L=K(\ell_1,\cdots,\ell_s)$ for $f\in K[X]$ having roots $\ell_i$, any $K$-homomorphism $\phi$ fixes the coefficients of $g\in K[X]$, hence permuting its roots, therefore $\phi(L)=L$. For $3\Rightarrow1$, take any irreducible $f\in K[X]$ that has a root $\alpha\in L$, and denote $L'$ its splitting field over $L$. Pick another root $\beta$, then by definition $\beta\in L'$. By isomorphic simple extension above there exists $$\psi:K(\alpha)\to K(\beta)\quad\text{s.t.}\quad\psi|_K=\id_K,\quad\psi(\alpha)=\beta$$By uniqueness of splitting field $\psi$ extends to $\tau:L\to L'$. By hypothesis, $\beta=\tau|_L(\alpha)\in L$. 
- **Normal closure**: a normal closure of finite $L/K$ is an extension $N/L$ s.t. 
  1. $N/K$ is normal, 
  2. $L\subset M\subset N$ with $N/K$ normal implies $M=N$.
	- **Existence & Uniqueness within $\mathbb C$**: if $L/K$ is a finite extension in $\mathbb C$, then normal closure $N\subset\mathbb C$ exists and is unique and finite.
	  **Proof**: by finiteness we can assume $L=K(\ell_1,\cdots,\ell_s)$ ([[Field]]), and consider $$f=\prod_{i=1}^s\mu_{\ell_i/K}$$Denote the splitting field of $f$ as $N\subset\mathbb C$, which is normal and finite by criterion above. Suppose $L\subset P\subset N$ with $P/K$ normal, then by normality $\ell_i\in P$ implies that $\mu_{\ell_i/K}$ splits over $P$, hence $f$ splits over $P$, and by minimality of splitting field we have $P=N$. For uniqueness, just invoke the definitions and minimality of splitting field.
## Separable extension
A extension $L/K$ is **separable** if all elements in $L$ are separable over $K$.
- **Separability**: irreducible $f\in K[X]$ is separable if all its roots in its splitting field are distinct. An element $\alpha$ algebraic over $K$ is separable if $\mu_{\alpha/K}$ is separable.
- **Primitive element theorem**: finite separable extension is simple.
  **Proof**: for $L/K$ finite and separable, if $K$ is finite, then so is $L$, and in this case $L^\times$ is cyclic ([[Field]]), hence $L/K$ is simple; if $K$ is infinite, by induction trick ([[Field]]) it suffices to show that $$L=K(\alpha,\beta)\quad\Longrightarrow\quad L=K(\theta)$$Denote the roots of $\mu_{\alpha/K}$ as $\alpha=\alpha_1,\cdots,\alpha_m$, and similarly for $\beta=\beta_1,\cdots,\beta_n$, which are distinct by assumption. Since $K$ is infinite, we can find $c\in K$ s.t. $$c\neq\frac{\alpha_i-\alpha_j}{\beta_s-\beta_t},\quad\forall 1\le i,j\le m,1\le s,t\le n,s\neq t$$Consider $$\theta=\alpha+c\beta,\quad h(X)=\mu_{\alpha/K}(\theta-cX)\quad\Longrightarrow\quad h(X)\in K(\theta)[X],\quad h(\beta)=0$$while $h(\beta_j)\neq0,\forall j\ge2$. It follows that $$\gcd_{K(\theta)[X]}(\mu_{\beta/K}(X),h(X))=X-\beta$$Consequently, $\beta\in K(\theta)$, and further $\alpha\in K(\theta)$, thus $K(\theta)=K(\alpha,\beta)=L$.